# Configuration Example
# Copy this file to .env and edit the values

# --- Serial Ports ---
# Define the serial port for uploading and monitoring.
# On Windows: COM3, COM4, etc.
# On Linux/Mac: /dev/ttyUSB0, /dev/cu.usbserial-xxx, etc.
BAR_UPLOAD_PORT=/dev/ttyUSB0
FLECHETTES_UPLOAD_PORT=/dev/ttyUSB1

# --- MAC Addresses ---
# Define the MAC address of each module.
# The build system will automatically inject the "Peer" MAC address.
# (e.g., when building BAR, it will use FLECHETTES_MAC_ADDRESS as the target)
# Format: XX:XX:XX:XX:XX:XX or XX-XX-XX-XX-XX-XX

# Address of the Bar Module (used by Flechettes to send back stats)
BAR_MAC_ADDRESS=48:E7:29:AC:92:F0

# Address of the Flechettes Module (used by Bar to send credits)
FLECHETTES_MAC_ADDRESS=48:E7:29:94:FD:BC
